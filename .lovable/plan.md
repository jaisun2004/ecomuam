# Re-add & Redesign Recommendations Screen

## 1. Sidebar entry
- In `src/components/Sidebar.tsx`, add a new section **OPTIMISATION** entry (or new INTELLIGENCE bucket) item:
  - `{ id: "recommendations", icon: Sparkles, label: "Recommendations", notify: true }`
- Place it under the INSIGHTS bucket (after Category Assortment) so it sits near other analytical views. Route already wired in `src/pages/Index.tsx` (`recommendations: RecommendationsView`).

## 2. Rebuild `src/views/RecommendationsView.tsx`

Replace existing layout (keep file, swap contents) with four stacked sections, all PepsiCo / Talabat–Noon–Noon Minutes–Carrefour / AED context.

### Section A — Top Strip: Performance Marketing KPIs + "New since yesterday" notification card
- 5-column grid:
  1. **Spend (MTD)** — AED value, WoW delta
  2. **ROAS** — value vs target
  3. **ACoS** — value vs target
  4. **CTR** — value, delta
  5. **New Recommendations** card (highlighted, primary accent, bell icon, pulse dot) — "7 new since yesterday", click scrolls to recommendations feed. Acts as the notification surface.

Use existing `KPICard` for the 4 KPIs and a custom highlighted card for the notification (primary gradient border, animated dot).

### Section B — KPI Target Progress Bars
PanelCard "Target Pacing — Month to Date" containing 3 horizontal progress bars:
- **ROAS**: current 4.2x / target 3.5x → green (met)
- **ACoS**: current 22% / target 18% → amber (over)
- **Spend**: current AED 6.84L / target AED 9.0L → blue (on pace)
Each row: label, current vs target numbers, `Progress` component from shadcn, status pill (Met / At Risk / Behind), one-liner context.

### Section C — Campaign Recommendations Feed (LLM-styled)
PanelCard "AI Recommendations for Active Campaigns". List of recommendation cards (5–6 mock items). Each card:
- Header: campaign name, platform pill (Talabat / Noon / Noon Minutes / Carrefour), confidence dots (5-dot scale), "Generated by AI" sparkles icon
- Body: contextual recommendation text (e.g. "Increase Pepsi 1L bid by 12% on Talabat — competitor Coca-Cola dropped bid 18% in Dubai Marina cluster")
- **Infographic strip**: small inline visual — mini bar chart, sparkline, or radial gauge (Recharts) showing the supporting signal (e.g. competitor bid drop, ROAS trend, share-of-shelf shift)
- **E-commerce KPI Warning Row** (when triggered): inline pills with warning icon for:
  - Availability issue (e.g. "3 dark stores OOS")
  - Pricing gap vs competitor (e.g. "Pepsi 1.5L priced 8% higher than Coca-Cola on Noon")
  - Low Share of Shelf (e.g. "SoS 14% — below 20% threshold")
  Each warning shows an **"Alert"** button → opens AlertDialog with details + "Acknowledge / Route to team" actions.
- Actions row: **View Changes** (primary) · Apply · Dismiss

### Section D — View Changes modal
Dialog opened from "View Changes" button shows a 2-column diff table:
| Field | Current | Recommended |
- Bid (AED), Daily Budget (AED), Target Keywords, Dayparting, Audience scope, Placement
Highlight changed rows (amber bg). Footer: Confirm Apply / Cancel.

## Technical details
- Components: `KPICard`, `PanelCard`, shadcn `Progress`, `Dialog`, `AlertDialog`, `Badge`, `Button`, Recharts (`BarChart`, `RadialBarChart`, `LineChart` minis), `lucide-react` (Sparkles, Bell, AlertTriangle, Package, DollarSign, TrendingDown).
- All colors via semantic tokens (`text-sw-green`, `bg-sw-amber-dim`, `text-primary`, etc.). No raw colors.
- Local state: `dismissed: Set<string>`, `appliedIds: Set<string>`, `openChangesId: string | null`, `openAlertId: string | null`.
- Toasts on Apply / Dismiss / Acknowledge via `sonner`.
- Mock data only — no backend. Currency AED, platforms restricted to Talabat / Talabat Pro / Noon / Noon Minutes / Carrefour, brand PepsiCo.

## Out of scope
- No real LLM call (visual treatment only).
- No changes to other views or routing logic beyond sidebar entry.
